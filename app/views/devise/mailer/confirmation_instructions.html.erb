<p>Welcome <%= @email %>!</p>

<p>You can confirm your account email through the link below:</p>

<p><%= link_to 'Confirm my account', "http://#{ENV['FRONTEND_HOST']}:#{ENV['FRONTEND_PORT']}/verify/#{@token}" %></p>

<%# // confirmation_url(@resource, confirmation_token: @token) %>
